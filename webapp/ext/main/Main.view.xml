<mvc:View
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:macros="sap.fe.macros"
    xmlns:html="http://www.w3.org/1999/xhtml"
    controllerName="project5.ext.main.Main"
>
    <f:DynamicPage
        id="Main"
        headerExpanded="true"
    >
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="{i18n>MainTitle}" />
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>
        <f:header>
            <f:DynamicPageHeader pinnable="true">
                <macros:FilterBar
                    id="FilterBar"
                    metaPath="/JobList/@com.sap.vocabularies.UI.v1.SelectionFields#filterBarMacro3"
                />
            </f:DynamicPageHeader>
        </f:header>
        <f:content>
            <macros:Table
                id="Table"
                rowPress=".onRowPress"
                metaPath="/JobList/@com.sap.vocabularies.UI.v1.LineItem#tableMacro2"
                filterBar="FilterBar"
                header="{i18n>backgroundJobs}"
                isSearchable="false"
                type="ResponsiveTable"
                enableFullScreen="true"
                enableExport="true"
                selectionMode="Multi"
                variantManagement="Page"
            >
                <macros:actions>
                    <Button
                        text="{i18n>btnRefreshSticker}"
                        type="Transparent"
                        press="onRefreshTable"
                    />
                    <Button
                        id="btnManage"
                        text="{i18n>btnManageSticker}"
                        type="Default"
                        press="onOpenManageMenu"
                    />
                    <Button
                        text="{i18n>btnMyJobsSticker}"
                        type="Emphasized"
                        press="onFilterOwnJobs"
                    />
                    <Button
                        text="{i18n>btnScheduleSticker}"
                        type="Success"
                        press="onCreateJob"
                    />
                    <Button
                        id="btnAnalytics"
                        text="{i18n>btnQuickLinkSticker}"
                        type="Transparent"
                        press="onOpenAnalyticsMenu"
                    />
                </macros:actions>
            </macros:Table>
        </f:content>
    </f:DynamicPage>
    <!-- Thêm Menu ẩn (ActionSheet) ở đây -->
    <mvc:dependents>
        <ActionSheet
            id="manageActionSheet"
            placement="Bottom"
        >
            <buttons>
                <Button
                    text="{i18n>btnReleaseSticker}"
                    press="onReleaseJob"
                />
                <Button
                    text="{i18n>btnRepeatSticker}"
                    press="onRepeatJob"
                />
                <Button
                    text="{i18n>btnCopySticker}"
                    press="onCopyWithRename"
                />
                <Button
                    text="{i18n>btnStopSticker}"
                    type="Attention"
                    press="onStopJob"
                />
                <Button
                    text="{i18n>btnDeleteSticker}"
                    type="Reject"
                    press="onDeleteJob"
                />
            </buttons>
        </ActionSheet>
        <ActionSheet
            id="analyticsActionSheet"
            placement="Bottom"
        >
            <buttons>
                <Button
                    text="{i18n>btnDashboardSticker}"
                    press="onOpenDashboard"
                />
                <Button
                    text="{i18n>btnAnalyzeSticker}"
                    press="onOpenAnalytics"
                />
            </buttons>
        </ActionSheet>
    </mvc:dependents>
</mvc:View>